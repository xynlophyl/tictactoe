class MiniMaxAgent extends searchAgent {

  getAction(vals) {
    let bestEval = Number.NEGATIVE_INFINITY;
    let bestAction = -1;

    for (let cell of this.grid.getLegalActions(vals)) {
      let val = -1
    }
  }
}